version: 4.0.70
steps:
  - prompt: Assert that the webpage is loaded.
    commands:
      - command: assert
        expect: the webpage is loaded
  - prompt: Assert that the webpage is loaded.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert that the webpage is loaded.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert that the webpage is loaded.
    commands: []
